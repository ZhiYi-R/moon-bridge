/**
 * Bauhaus control adapters — kept under Material* names so feature imports stay stable.
 */
export {
  FilledButton as MaterialFilledButton,
  IconButton as MaterialIconButton,
  OutlinedButton as MaterialOutlinedButton
} from "./ui/Button";
